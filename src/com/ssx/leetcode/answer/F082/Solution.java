package com.ssx.leetcode.answer.F082;

import com.ssx.util.ListNode;

public class Solution {
    public ListNode deleteDuplicates(ListNode head) {
        ListNode declare = head;
        int val = 0;
        while(head.next != null){
            val = head.val;
            if(head.next.val == head.val){
                
            }
        }
        return new ListNode();
    }
}
